/** Shared pagination / list defaults (server-safe — do not put in "use client" modules). */
export const INVOICES_PAGE_SIZE = 15;
export const DEFAULT_PAGE_SIZE = 15;

/** Default brand logo served from /public */
export const DEFAULT_LOGO_PATH = "/logo.png";
